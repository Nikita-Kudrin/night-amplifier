# Cross-compilation configuration for the `cross` tool.
# See: https://github.com/cross-rs/cross

[target.aarch64-unknown-linux-gnu]
# fitsio-src needs autotools/cmake to compile cfitsio from source
pre-build = [
    "apt-get update && apt-get install -y autoconf automake libtool cmake"
]
